了解REST：
	1.每个URI代表一种资源
	2.每中资源用唯一的URI，其表现形式的不同可以在HTTP请求中指定
	3.URI中不包含动词，动词表现在HTTP协议中
	4.HTTP四种动词：GET  POST PUT DELETE

综上，使用Spring MVC框架处理请求，并返回Json格式的数据，实现前后台分离

---------------------------
大体结构：
controller层：中间层，得到url后调用其他层，返回结果

dao层：数据库查询

entity层：数据库表对应的类

service层：实现具体业务处理

content层：数据的再包装

---------------------------
根据员工id及月份等到具有 员工ID 员工姓名 员工上下班时间 员工工作总时长 员工是否迟到早退，信息的表
---------------------------
查询方法：根据员工ID写一个sql语句在多张表中查询，直接返回结果OR多次查询，在后台部分组装成考勤表。
-------------------------------
签到表设计：员工id，年，月，日，时间  5个字段组成主键，确定唯一的一条信息
----------------------------------
总结：由于基本不扎实，在了解REST风格，选择Spring MVC框架，学习和搭建框架上耗费了较多时间。时间仓促，在SpringMVC和JSON的协同上，学习不够。整个考勤API的设计上，后台部分有很多可以优化的地方，以增加运行效率。程序大体处理过程和需求的解决方法已经具备，很多小的细节还需完善。由于时间原因，无法一一调试，因此在运行方面还有诸多问题。
-----------------------------------------
7.26 更新目标：部署说明，单元测试，使用数据库，修改url，考勤表日期字段，考勤表逻辑部分优化，异常考勤表优化

修改url：根据理解，某员工的某月考勤表url应该是：    /{workerID}/{mounth}

签到表设计： 主键（ID，自增长） 员工ID(WorkerID) 打卡时间(ClockTime dateTime类型)

增加Hibernate框架，实现表和实体类一一对应。

问题1：模块间耦合度太高，增加一个year参数需要更改太多方法

问题2:数据库检索次数太多，存在性能问题

---------------------------------------
7.28完成工作

1.代码注释完善
2.增加了测试单元，并测试
3.增加了Hibernate框架，链接数据库
4.考勤表字段修复
5.考勤表逻辑完善和优化（未测试）
6.修改数据库检索问题，只用分别查询三张表

7.29 完成工作 url测试，数据库测试，功能测试，异常考勤表逻辑优化
测试用的地址
http://localhost:8080/sdbs/attendence/attendencealltable?year=2016&&month=07